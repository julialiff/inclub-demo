<%= form_for(festum) do |f| %>
  <% if festum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(festum.errors.count, "error") %> prohibited this festum from being saved:</h2>

      <ul>
      <% festum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
