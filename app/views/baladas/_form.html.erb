<%= form_for(balada) do |f| %>
  <% if balada.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(balada.errors.count, "error") %> prohibited this balada from being saved:</h2>

      <ul>
      <% balada.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
