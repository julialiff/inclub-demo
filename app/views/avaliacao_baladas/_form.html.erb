<%= form_for(avaliacao_balada) do |f| %>
  <% if avaliacao_balada.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(avaliacao_balada.errors.count, "error") %> prohibited this avaliacao_balada from being saved:</h2>

      <ul>
      <% avaliacao_balada.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
